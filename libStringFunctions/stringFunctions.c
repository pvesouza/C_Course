#include "stringFunctions.h

int numberOfCharacteresInString(char* str, char character)
{
    int i = 0, frequency = 0;

    for (i = 0; str[i] != '\0'; i++)
    {
        if (character == str[i])
        {
            frequency++;
        }
    }

    return frequency;
}

int removeNonAlphaCharacters(char *str)
{
    int i = 0, j = 0;

    for (i = 0; str[i] != '0'; i++) {

        while()
    }
}
